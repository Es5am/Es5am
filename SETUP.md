# Setup — Essam Mohamed GitHub Profile

## 1) Create the profile repository

Create a **public** repository named exactly:

`Es5am`

GitHub recognizes a repository with the same name as the username as the profile README repository.

## 2) Upload the files

Copy this structure into the repository:

```text
Es5am/
├── README.md
└── assets/
    ├── header.svg
    └── footer.svg
```

The `README.md` from this package is ready to be used as the profile README.

## 3) Update the repository

Commit to the default branch (normally `main`).

## 4) Check the result

Open:

https://github.com/Es5am

## Notes

- The profile uses dynamic GitHub analytics widgets. They are intentionally remote so the numbers can update automatically.
- The custom header/footer are local SVG files, so the visual identity does not depend on an image-hosting service.
- Your CV and portfolio claims were kept conservative: the profile highlights the project metrics already present in your materials.
- The portfolio URL is `https://es5am.github.io/MyPortfolio/`.
